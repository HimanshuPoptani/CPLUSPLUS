#include<iostream>
#include<cmath> // this is cmath inbuild function
using namespace std;

int main(){
    cout << sqrt(64); //Square root of 64
    cout << round(2.6); // Round off
    cout << log(2); // Logging

    return 0;
}