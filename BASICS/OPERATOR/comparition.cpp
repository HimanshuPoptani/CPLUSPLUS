#include <iostream>

using namespace std;

int main() {
    int x = 5;
    int y = 10;

    cout << (x>y) << endl; // Returns 0 because false
    cout << (x<y); // Returns 1 because true 

    return 0;
}
