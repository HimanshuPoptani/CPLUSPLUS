#include<iostream>
using namespace std;

int main(){
    // Constants are called when you don't want variables to change
    const float PI = 3.14;
    cout << "Value of PI can't be changed in this code which is = " << PI;

    return 0;
}